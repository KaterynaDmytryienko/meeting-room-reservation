package meeting.room.system.enums;

public class ReservationRules {
    public static final int MAX_ADVANCE_BOOKING_DAYS = 30;
    public static final int MIN_RESERVATION_LENGTH = 1;
    public static final int MAX_RESERVATION_LENGTH = 8;
    public static final int MAX_CANCELLATION_TIME = 2; // max time when user can cancel reservation is 2 hours before it
}
